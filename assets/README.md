# assets/

Place **profile.jpg** here — this is the profile photo shown in the iMessage header.

Recommended size: at least 200×200px, square crop, JPG or PNG.
Rename your file to `profile.jpg` (or update the `src` in `index.html`).

If the file is missing, the header will show the initials **BA** in a purple gradient circle instead.
